<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/darkButton"
        android:title="darkButton"
        app:showAsAction="always"
        android:icon="@drawable/baseline_dark_mode_20"/>

    <item
        android:id="@+id/textButton"
        android:title="textButton"
        app:showAsAction="always"
        android:icon="@drawable/baseline_format_size_18"/>

    <item
        android:id="@+id/addTaskButton"
        android:title="addTaskButton"
        app:showAsAction="always"
        android:layout_height="30dp"
        android:layout_width="30dp"
        android:icon="@drawable/baseline_add_circle_24"/>

</menu>